/**
 * Payment queries - re-export from modular structure
 * Original file (842 lines) has been split into modules
 */

// Re-export everything from the modular structure
export * from './queries/index'

// For backward compatibility, also export PaymentQueryService directly
export { PaymentQueryService } from './queries/index'